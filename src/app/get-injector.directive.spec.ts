import { GetInjectorDirective } from './get-injector.directive';

describe('GetInjectorDirective', () => {
  it('should create an instance', () => {
    const directive = new GetInjectorDirective();
    expect(directive).toBeTruthy();
  });
});
